function nameChamp(name) {
  return `The name of this champ is: ${name}`;
}
function nameType(type) {
  return `This tipe this mage is: ${type}`;
}

const champ = {
  nameChamp,
  nameType,
};

export default champ;
